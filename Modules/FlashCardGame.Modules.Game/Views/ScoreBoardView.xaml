<UserControl x:Class="FlashCardGame.Modules.Game.Views.ScoreBoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <materialDesign:Card VerticalAlignment="Center" HorizontalAlignment="Center"  Width="400" Height="100" materialDesign:ShadowAssist.ShadowDepth="Depth5" Padding="32" Margin="16">

        <StackPanel Orientation="Horizontal">
            <materialDesign:PackIcon Kind="Score"  Height="32" Width="32" />
            <Label Content="Your current score" />
            <Label Content="{Binding TotalScore}" />
            <Label Content="Your correct" />
            <Label Content="{Binding CorrectCount}" />
            <Label Content="Your wrong" />
            <Label Content="{Binding WrongCount}" />
        </StackPanel>
    </materialDesign:Card>
</UserControl>